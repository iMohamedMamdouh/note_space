plugins {
    id("com.android.application")
    id("kotlin-android")
    // يجب تطبيق Flutter Gradle Plugin بعد Android و Kotlin Gradle plugins.
    id("dev.flutter.flutter-gradle-plugin")
    id("com.google.gms.google-services")
}

android {
    namespace = "com.example.note_space"
    compileSdk = flutter.compileSdkVersion

    // تعيين إصدار NDK المطلوب هنا
    ndkVersion = "27.0.12077973"  // تحديد إصدار NDK الصحيح

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_11.toString()
    }

    defaultConfig {
        applicationId = "com.example.noteSpace"
        minSdk = 23  // تعيين minSdkVersion إلى 23 أو أعلى
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.getByName("debug")
        }
    }
}

flutter {
    source = "../.."  // تأكد أن هذه المسار يشير إلى مصدر مشروع Flutter الصحيح
}

dependencies {
    implementation(platform("com.google.firebase:firebase-bom:33.13.0"))
    implementation("com.google.firebase:firebase-analytics")
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"  // تأكد من صحة نسخة Kotlin
}

apply plugin: 'com.google.gms.google-services'  // تطبيق Google Services plugin
